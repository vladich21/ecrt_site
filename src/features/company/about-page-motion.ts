import { scrollRevealFade, scrollRevealFadeUp, scrollRevealStagger, motionEase, heroCopyMotion, fadeUp, sectionReveal } from "@/shared/motion/presets";

export { motionEase as ease, heroCopyMotion, fadeUp, sectionReveal };

export const fadeUpItem = scrollRevealFadeUp;
export const pillarStagger = scrollRevealStagger;
export const pillarItem = scrollRevealFade;
